
<li class="dropdown">
    <a href="#"><span>Catégories</span> <i class="bi bi-chevron-down"></i></a>
    <ul>
        {% for categorie in categories %}
            <li><a href="#">{{ categorie.nom }}</a></li>
        {% else %}
            <li><a href="#">Aucune catégorie</a></li>
        {% endfor %}
    </ul>
</li>
