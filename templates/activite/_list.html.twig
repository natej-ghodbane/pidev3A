{% if activities %}
    <ul class="list-group">
        {% for activity in activities %}
            <li class="list-group-item d-flex justify-content-between align-items-start">
                <div>
                    <strong>{{ activity.nomActivite }}</strong><br>
                    📅 {{ activity.date|date('d/m/Y') }} à {{ activity.heure }}<br>
                    Statut:
                    {% if activity.statut == 'active' %}
                        <span class="badge bg-success">Active</span>
                    {% elseif activity.statut == 'inactive' %}
                        <span class="badge bg-warning text-dark">Inactive</span>
                    {% else %}
                        <span class="badge bg-secondary">Terminée</span>
                    {% endif %}
                </div>


            </li>
        {% endfor %}
    </ul>
{% else %}
    <p class="text-muted text-center">Aucune activité disponible pour cette destination.</p>
{% endif %}
